const DataManager = ({ className = "className" }) => {
  return (
    <>
      <div className={`${className} flex w-[80%]`}>
        <h1>Data Manager</h1>
      </div>
    </>
  );
};

export default DataManager;

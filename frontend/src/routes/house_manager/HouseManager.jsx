const HouseManager = ({ className = "className" }) => {
  return (
    <>
      <div className={`${className} flex w-[80%]`}>
        <h1>House Manager</h1>
      </div>
    </>
  );
};

export default HouseManager;

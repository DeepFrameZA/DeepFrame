const DocumentManager = ({ className = "className" }) => {
  return (
    <>
      <div className={`${className} py-6 max-w-[95%] relative top-14 flex`}>
        <h1>Document Manager</h1>
      </div>
    </>
  );
};

export default DocumentManager;

const ListView = ({ className = "className" }) => {
  return (
    <>
      <div className={`${className} flex w-[80%]`}>
        <h1>List View</h1>
      </div>
    </>
  );
};

export default ListView;
